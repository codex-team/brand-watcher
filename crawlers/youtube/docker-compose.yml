version: "3.3"
services:
    redis:
      image: redis
      volumes:
        - ./redis_data:/data
      ports:
        - "6379:6379"
    youtube-crawler:
      build:
        dockerfile: ./Dockerfile
      command: python ./main.py
